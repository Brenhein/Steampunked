<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.msu.harr1332.project1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WaitActivity" />
        <activity android:name=".SteampunkedViewOnline" />
        <activity android:name=".GameBoardOnlineActivity" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".LobbyActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GameOverActivity" />
        <activity android:name=".GameBoardActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>